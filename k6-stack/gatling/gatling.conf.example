gatling {
  data {
    writers = [console, file, influxdb]
  }
}

# InfluxDB settings for gatling-to-influxdb plugin
influxdb {
  url = "http://localhost:8086"
  database = "gatling"

  # Optional: add custom tags
  # tags {
  #   environment = "dev"
  #   application = "my-app"
  # }
}