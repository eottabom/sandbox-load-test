spring:
  application:
    name: perf-backend

  datasource:
    url: jdbc:h2:file:./data/perf
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
